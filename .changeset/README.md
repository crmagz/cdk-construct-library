# Changesets

Use changesets to declare which workspace packages should release and what semver bump they need.

```sh
npm run changeset
```

Select one package for a service-only release or multiple packages when a shared change should move together.
