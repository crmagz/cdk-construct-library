# EKS Release Preview

This page exists only to validate the Changesets release workflow against mixed
conventional commit types on the EKS pull request.

The package release is controlled by `.changeset/*.md` files. Conventional commit
titles keep the branch history readable, but they do not independently create
package releases.
